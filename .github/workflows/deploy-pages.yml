name: deploy-pages

on:
  push:
    branches:
      - main
      - codex/unified-model-foundation
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: github-pages
  cancel-in-progress: true

jobs:
  build:
    if: vars.GITHUB_PAGES_ENABLED == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip
      - name: Install model
        run: pip install .
      - name: Build static research dashboard
        env:
          MLB_MODEL_DEPLOYMENT_NOTICE: >-
            Static research snapshot. Live odds and warehouse credentials are not connected;
            no displayed state is a wager instruction.
        run: |
          mkdir -p _site
          python -m mlbmodel.report.app \
            --game HOU@DET \
            --data-dir deployment_data \
            --out _site/index.html \
            --no-fetch
      - name: Configure Pages
        uses: actions/configure-pages@v5
      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: _site

  deploy:
    if: vars.GITHUB_PAGES_ENABLED == 'true'
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
